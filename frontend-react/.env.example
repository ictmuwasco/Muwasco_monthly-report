# === VITE ===
VITE_API_URL=http://localhost:8000/api/v1
# When true, axios uses the /api dev proxy (recommended for local dev).
# When false, it calls VITE_API_URL directly (see vite.config.js proxy note).
VITE_USE_PROXY=true